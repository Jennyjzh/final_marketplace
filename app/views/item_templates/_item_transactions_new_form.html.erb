<form action="/create_transaction_from_item" method="post">

  <div class="form-group">
    <label for="buyer_id">
      Buyer
    </label>

    <%= select_tag(:buyer_id, options_from_collection_for_select(User.all, :id, :email, @transaction.buyer_id), :class => "form-control") %>
  </div>

  <div class="form-group">
    <label for="seller_id">
      Seller
    </label>

    <%= select_tag(:seller_id, options_from_collection_for_select(User.all, :id, :email, @transaction.seller_id), :class => "form-control") %>
  </div>

  <input type="hidden" name="item_id" value="<%= @item.id %>">

  <div class="form-group">
    <label for="sale_price">
      Sale price
    </label>

    <input type="text" id="sale_price" name="sale_price" class="form-control" value="<%= @transaction.sale_price %>">
  </div>

  <div class="form-group">
    <label for="notes">
      Notes
    </label>

    <input type="text" id="notes" name="notes" class="form-control" value="<%= @transaction.notes %>">
  </div>

  <button class="btn btn-block btn-outline-secondary">
    Create transaction
  </button>
</form>
